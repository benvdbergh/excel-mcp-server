{
  "mcpServers": {
    "excel-local": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${workspaceFolder}",
        "excel-com-mcp",
        "stdio"
      ]
    }
  }
}
